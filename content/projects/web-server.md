[project]
name = "web-server"
tagline = "A blatant rip-off of (not really working) Nginx built in Rust."
link = "https://github.com/code-wolf-byte/reverse-proxy-server/"
status = "archived"
year = "2023"
stack = ["Rust", "Tokio"]

[content]
A reverse proxy written in Rust on top of Tokio, built to learn how one works
rather than to replace the one that does.
